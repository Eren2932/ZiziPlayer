# ZiziPlayer 6.22.1 / ZiziResolve 1.6.1 — «фон знает своё место»

## Что произошло в 1.6

1.6 не замедлила извлечение ссылки. Она забила канал и единственное ядро сервера прогревом
БАЙТОВ и тем самым замедлила всё остальное. Журнал за два часа живого пользования 30.08:

| | n | средн. | медиана | p90 | макс |
|---|---|---|---|---|---|
| живые резолвы | 190 | 17.0 с | 16.3 с | 25.8 с | 53.2 с |
| фоновый прогрев | 153 | 61.3 с | 23.2 с | 172 с | 252 с |
| отказы клиентов | 152 | 6.7 с | 1.5 с | 12.9 с | 175 с |

Чистая цена одного клиента на том же сервере, когда его никто не трогал: **mweb 6.3 с**.
Байты: скачано 329 МБ, отдано телефону 79 МБ; залито 120 треков, пригодились 21.

Отсюда всё и читается:

* `resolve_avg_ms = 61153` в /stats 1.6 — **длина очереди прогрева**, а не цена резолва:
  `t0` в `_try_route` стоял ДО взятия слота. Метрика была моей ошибкой.
* живой тап платил 17 с вместо 6 с, потому что yt-dlp внутри `to_thread` делил и GIL, и полосу
  с 4–12 соединениями фоновой заливки; его запросы упирались в `socket_timeout=20` и уходили в
  ретрай (`retries=2`). Средний отказ mweb в журнале — 22.5 с ровно по этой причине.
* приоритетный слот 1.6 работал: живой запрос действительно пропускали вперёд. Только пропускали
  его в дом, где уже пожар.

## Сервер: 1.6 → 1.6.1

* **байты греются только по явной просьбе.** `/prefetch?ids=...&bytes=1` — очередь
  воспроизведения; без `bytes=1` греется только ссылка (один запрос, живёт до шести часов).
* **наследование приоритета на локе по треку.** Найденная в 1.6 инверсия: прогрев брал
  `_locks[id]` и лишь потом уступал живому. Тапнув по треку, который в эту секунду греется,
  человек становился в очередь за задачей, которая специально не работает, пока он ждёт. Теперь
  живой запрос объявляет о себе ДО лока, а прогрев по этому же треку мгновенно повышается до
  живого. Счётчик — `promoted` в /stats.
* **терпение фона конечно** (`ZIZI_BG_YIELD_MAX=45`): уступать вечно значит не прогреть ничего,
  то есть вернуть холодный тап.
* **фоновая заливка уступает канал** живому резолву на время его работы и тянет
  `ZIZI_FETCH_WORKERS_BG=2` воркерами; одновременных фоновых заливок — `ZIZI_FILL_SLOTS=1`.
  Живая заливка (человек слушает сейчас) не ждёт никогда и берёт полные `ZIZI_FETCH_WORKERS`.
  Фоновая заливка, которую начал слушать человек, немедленно становится живой.
* **таймауты в окружении**: `ZIZI_SOCKET_TIMEOUT=12`, `ZIZI_YDL_RETRIES=1` вместо 20 и 2
  намертво. Клиент отвечает за 6 секунд или не отвечает вообще.
* **честные метрики**: `live_wait_avg_ms` / `live_work_avg_ms` / `live_count` и такие же
  `warm_*`. Первое — цена очереди, второе — цена yt-dlp. Складывать их в одно среднее нельзя.
* **липкий победитель переживает перезапуск**: `_sticky` и `client_wins` пишутся в
  `ZIZI_CACHE_FILE` (формат `{"v":2,...}`, плоский файл от 1.6 читается как раньше).
* в журнале теперь `resolved ... in 17.0s (очередь 11.0 + работа 6.0, ...)` — видно сразу, чья
  это секунда.

## Клиент: 6.22.0 → 6.22.1 (versionCode 41)

* `ZiziResolve.prefetch(ids, bytes = false)`: поиск и открытый альбом греют ССЫЛКИ,
  `PlayerController.prefetchAhead` — байты. Кулдаун дедупликации раздельный для двух режимов,
  иначе прогретая из поиска ссылка блокировала бы прогрев байтов в очереди.
* Kotlin тронут в трёх файлах, схема БД 6 без изменений.

## Проверено

17 проверок логики на настоящем `zizi_resolver.py` с подменёнными зависимостями: обгон живым
запросом трёх прогревов, наследование приоритета, конечность терпения фона, снятие инверсии на
локе, `/prefetch` без байтов и с байтами, отсутствие очереди у живой заливки, лимит фоновых
заливок, раздельные счётчики, круговорот sticky через файл и чтение формата 1.6, таймауты из
окружения. Плюс `check_kotlin_syntax.py` — 0, `check_static_refs.py` — 0, `bash -n`, `ast.parse`.

Сети в песочнице нет: логика и арифметика проверены, живой замер за тобой.
