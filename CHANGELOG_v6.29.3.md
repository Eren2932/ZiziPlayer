# 6.29.3

Четыре знака и один блок по референсам. Продолжение 6.29.2: тот же метод — маска эталона,
параметрическая модель, подгонка по IoU, — но здесь под него попал не один глиф, а весь верх
плеера, поле поиска и вход в импорт.

## «Перемешать»

Знак перерисован целиком. Прежний рисунок был не той же структуры, что референс: две сплошные
S-кривые во всю ширину, которые просто накладывались в середине. На тёмном фоне пересечение
читалось как узел — линии слипались в одно пятно, и знак терял смысл. В оригинале перехлёст
показан **разрывом**: нижняя стрелка проходит под верхней и потому прервана. Это и есть знак,
всё остальное — обвязка.

Модель: короткий хвост, кубика, горизонтальная планка, наконечник. Зеркальная симметрия
относительно y=12. Числа подгонкой по IoU на маске 74x66, **итог 0.935** (в 6.29.2 у «повторять»
было 0.946).

| | было | стало |
|---|---|---|
| Хвосты слева | ±4.4 от оси | ±5.187 |
| Планки справа | ±4.4 | ±4.662 |
| Крыло наконечника | 3.69 под 40.6° | 3.144 под 42.5° |
| Разрыв | нет | t=[0.333..0.700] нижней кривой |

Хвосты слева разнесены сильнее планок справа — это не огрех замера, а свойство оригинала: так
сходящиеся линии не слипаются на входе в кривую.

Габарит 17.99 из 24 против прежних 18.05. Вес и кегль не изменились, поэтому размеры на местах
вызова не тронуты, и новый рисунок сам разошёлся по всем четырём точкам: шапка медиатеки,
строка плейлиста, «Перемешать» в поиске, транспорт плеера. Источник один — разнобоя быть не может.

Активное состояние приведено к «повторять»: фирменный оранжевый плюс точка под знаком, круглая
подложка снята. Подложка спорила с новым рисунком — на включённом режиме разрыв тонул в подсветке.

**Оранжевый только в плеере, и это осознанно.** Три остальные кнопки зовут `shuffleAll()` — это
разовое действие, а не режим. Гореть там нечему: подсветка мигнула бы на кадр и погасла, обещая
состояние, которого нет.

## Троеточие

Единственный знак набора со своей шириной линии: 3.65 при шаге 7.775 против общих 2.05 при 6.6.
Отношение диаметра точки к шагу в референсе 0.47, у нас было 0.31 — знак был **вдвое жиже**
оригинала при том же габарите и читался как пунктир. Правило единого веса нарушено намеренно:
вес точки задаёт площадь пятна, а не толщина штриха, и подчинять её общему 2.05 значит рисовать
другой знак. В шапке плеера кегль 22 -> 24, общая высота выходит 18.92 dp как в эталоне.

## Шеврон закрытия плеера

Кегль 30 -> 31: габарит в референсе 19.65 dp, у нас было 19.06.

Обводка осталась на весе набора — 2.64 dp против 2.91 у эталона. Расхождение 0.27 dp, на экране
1080 это 0.7 пикселя. Заводить ради него четвёртый знак-исключение я не стал: шеврон живёт не
только в плеере, а в выпадающих списках его вес верный. Если разница видна — правится одной
строкой, отдельным `heavy`-вариантом на 2.293.

## Поле поиска

Радиус угла 8 -> 3.5 dp. Прежний был вдвое круглее оригинала, плашка читалась как «пилюля».
Высота 52 -> 49.5, отступ до лупы 14 -> 9.5, просвет до текста 10 -> 11, подсказка 16sp SemiBold
-> 17sp Bold.

Лупа 22 -> 29 dp. Пропорции знака у нас и в референсе почти совпадают (обводка к ширине 0.112
против 0.118) — расходился только размер, наша была на треть мельче.

## Импорт

Был плиткой `act:import` и уезжал в самый хвост: сборка списка возвращает `pinned + body + tiles`.
То есть попадал тем дальше, чем больше у человека музыки — притом что нужен ровно наоборот, когда
медиатека ещё пустая. Стал баннером над списком, в обоих режимах (список и сетка).

Геометрия с референса: поля 16.4, высота 82.2, радиус 16.4, отступ до знака 25.5, знак 23.5,
просвет до текста 25.8, шеврон 9.1x16 с правым полем 35.3. Радиус угла подгонкой дуги по вырезу
дал 45 px, а не 40 — на глаз это разные радиусы.

Текст свой: «Импортируй плейлист из файла или ссылки». Форма фразы как в эталоне — две строки,
жирный, — но обещать импорт «из других приложений» мы не можем, у нас файл и ссылка.
