# ZiziPlayer 6.4.0 — цвет из обложки, и список, который не лагает

versionCode 11 · versionName 6.4.0 · базируется на 6.3.0 (versionCode 10)

Две темы: (1) фон, который реально повторяет обложку, (2) скролл.

---

## 1. Фон под цвет обложки

### Что было сломано

6.3 строил фон в **HSV**. HSV не перцептивен: «value 0.24» — это чистый navy на одном
тоне и оливковая грязь на другом. Плюс каждый стоп градиента брал **свой** swatch, поэтому
тон уезжал сверху вниз: коричневый у названия, зелёный у кнопок. Отсюда и «полная хрень
с цветами» — это не подбор констант, это была неправильная модель цвета.

### Что показал референс (замеры, не на глаз)

Взял колонку пикселей у левого края капчи, перевёл в Oklch, три разные обложки
(стабильные кадры 60 / 240 / 310 из 395):

| y | 0.06 | 0.10 | 0.16 | 0.24 | 0.34 | 0.50 | 0.67 | 0.83 | 0.90 |
|---|---|---|---|---|---|---|---|---|---|
| тёмная обложка (L) | .144 | .173 | **.193** | **.193** | .184 | .168 | .152 | .113 | .097 |
| красная обложка (L) | .120 | .161 | **.182** | **.182** | .161 | .145 | .120 | .077 | .083 |
| светлая обложка (L) | .398 | .540 | **.654** | .618 | .546 | .428 | .289 | .148 | .113 |

Три вывода:

1. **Тон постоянный по всему экрану** — дрейф 28–40° на тёплой обложке, 41–44° на красной.
   Один тон, и это тон обложки.
2. **Хрома идёт за светлотой: C = k·L**, и `k` — свойство обложки, а не константа:
   0.057 на приглушённой, **0.27** на насыщенной красной. Никакой «кап насыщенности 0.44»
   этого не выражает — именно поэтому приглушённые и неоновые обложки давали один и тот же экран.
3. **Пик яркости на y = 0.16…0.24 — ЗА ОБЛОЖКОЙ**, и дальше экран темнеет до самого низа.

> Отдельно: в `CHANGELOG_v7.0.md` / `v7.1.md` написано, что пик у референса на **68 %**,
> под кнопками. По капче — нет. Любой ramp, который светлеет к кнопкам, перевёрнут, и низ
> экрана выглядит подсвеченным снизу. В коде исходника v7.0/v7.1 нет вообще (в зипе лежит
> 6.3.0, `versionCode 10`, HSV-ramp), так что 6.4 сделана от 6.3 и от замеров.

### Что теперь в коде (`ui/theme/ZiziTheme.kt`, переписан)

* Полный Oklab/Oklch без зависимостей (~60 строк матриц Ottosson).
* Форма ramp'а из замеров: позиции `0 / .20 / .36 / .52 / .68 / .84 / 1.0`,
  относительные светлоты `0.66 / 1.00 / 0.91 / 0.81 / 0.68 / 0.50 / 0.40`.
* **Один тон** на весь экран: круговое среднее по swatch'ам с весом `C²`, поэтому один серый
  swatch не тянет цветную обложку в сторону.
* `k = 0.05 + 1.10·C_peak`, клип `0.05…0.30`. Подогнано по замеру: приглушённая → 0.073
  (замер 0.057), насыщенная красная → 0.262 (замер 0.27).
* `L_peak = 0.075 + 0.30·L_обложки`, клип по теме. Подогнано: красная → 0.195 (замер 0.182),
  приглушённая → 0.180 (замер 0.193). **Тёмная обложка теперь даёт тёмный экран, светлая —
  светлый**; в 6.3 яркость была прибита гвоздями, и «смена темы» сводилась к сдвигу тона.
* Потолок 0.34 — сознательно ниже референса (он уходит до 0.654 на белой обложке): дальше
  вторичный текст перестаёт держать 4.5:1. Проверено численно на 5 типах обложек,
  худший контраст вторичного текста по самой светлой полосе — **6.1:1**, основного — 8.2:1.
* Вне гаммы отдаётся **хрома, шагами по 6 %, никогда не тон и не светлота**. Полоса, которая
  темнеет, больше не уползает в другой цвет.
* Ч/б обложка **специально** оставляет тему нетронутой: раньше усиление шума сенсора в тон
  красило приложение в бирюзу.
* Плашки/шиты/чипы больше не фиксированные хексы — они привязаны на шаг выше пика,
  иначе светлая обложка их проглатывала.
* Радиальный glow снижен с 0.26 до 0.11 (плеер) и с 0.10 до 0.05 (библиотека): цвет теперь
  разносит сам ramp, а два наложенных washes и выжигали тёплые обложки.
* Кроссфейд 380 мс FastOutSlowIn (у референса видимый переход ~3 кадра капчи 16.7 fps).
  Значения читаются **внутри `drawBehind`** — смена трека стоит перерисовки, а не рекомпозиции.

Публичный API темы не менялся: `basePalette` / `tintedWith` / `withAccent` / `ZiziBackdrop` /
`LocalPalette`. Формат сохранённых swatch'ей (`ArtSwatches.pack`) **не менялся** — переиндексации
библиотеки не будет.

---

## 2. Скролл списка

### Главная причина лагов

`ArtworkCache.load` шёл в 4 шага, и шаг 2 — **декод с диска** — был подписан как «дешёвый»
и работал **без семафора** на `Dispatchers.IO`, а это 64 потока. Один флик по списку из 400
строк запускал до 64 одновременных `BitmapFactory.decodeFile`. Они не спят на IO — они жгут CPU
и аллоцируют, конкурируя за кадры с тем самым списком, для которого декодируются. При такой
картине список не мог быть плавным, сколько бы ни оптимизировали остальное.

* `diskGate = Semaphore(3)` вокруг шага 2 (`ui/components/Artwork.kt`). Внутри — повторная
  проверка памяти: пока ждал permit, соседняя корутина уже могла декодировать ту же обложку.
* Строки **не декодируют под пальцем**: пока `ArtworkCache.isBusy()`, `rememberArtwork` ждёт
  тиками по 60 мс (ограниченно, максимум ~720 мс, чтобы «залипший» флаг никогда не оставил
  список серым). Улетевшие строки отменяются выходом из композиции и не стоят ничего.
* Трек, про который уже известно «обложки нет», **больше не стартует корутину** вообще.
  Раньше это было 400 корутин на флик, чтобы 400 раз вернуть `null`.
* Префетчер **целится в видимое окно** (`ArtworkPrefetcher.focusOn`, `LibraryScreen` шлёт
  `firstVisibleItemIndex`) и переприцеливается на каждом элементе. Раньше проход шёл от трека 0
  до конца: пролистал на трек 2000 — сначала грузятся 1999 обложек, которые ты не увидишь.
  Именно поэтому на большой библиотеке список «никогда не догружался».

### Очередь (`Очередь 112` из капчи)

`LazyColumn` был вложен в `verticalScroll`-родителя — классическая ловушка Compose: два
скроллера делят один жест, ленивый список теряет переиспользование строк. Очередь из 100 треков
скроллилась хуже, чем библиотека из 3000.

* `ZiziSheetScaffold(scrollable = false)` — очередь ведёт свой `LazyColumn` сама.
* Фиксированная высота строки 64.dp вместо `vertical = 8.dp` padding → строки не перемеряются.
* `contentType = "queue"` → переиспользование всего поддерева строки, а не пересборка.
* Максимальная высота 62 % экрана вместо жёстких 460.dp (на маленьких экранах шит вылезал).

### Мелочи, которые складываются

* `MiniProgressLine` разворачивал `progress` через `by` в композиции — весь мини-плеер
  рекомпозился **дважды в секунду всё время воспроизведения**. Теперь `.value` читается внутри
  `drawBehind`.
* `drawEqualizerBars` аллоцировал `listOf(...)` + три boxed `Float` **на каждом кадре** анимации
  для каждой играющей строки. Развёрнуто в три вызова.

---

## Что НЕ делал сознательно

* Не добавлял `androidx.profileinstaller`: без реального `baseline-prof.txt` он не даёт ничего,
  а новая зависимость — это риск на билде, который я не могу проверить.
* Не менял формат `ArtSwatches` (можно было бы хранить светлоту обложки отдельно и брать её
  точнее, чем взвешенное среднее по трём swatch'ам) — это заставило бы переиндексировать всю
  библиотеку. Оставил на 6.5, если решишь.
* Компилятором не проверял: Android SDK / Gradle в моей песочнице нет, сеть закрыта. Правки
  локальные, публичные подписи не менялись, баланс скобок и все места использования полей
  палитры сверены. Прогони `./gradlew assembleDebug` — если что-то не так, это будет
  одна строка, кинь мне лог.

## Изменённые файлы

```
app/build.gradle.kts                              versionCode 11, versionName 6.4.0
app/src/main/java/app/ziziplayer/MainActivity.kt  glow 0.26→0.11 / 0.10→0.05
app/src/main/java/app/ziziplayer/ui/theme/ZiziTheme.kt         переписан (Oklch)
app/src/main/java/app/ziziplayer/ui/components/Artwork.kt      diskGate, no-art fast path, focusOn
app/src/main/java/app/ziziplayer/ui/components/Common.kt       эквалайзер без аллокаций
app/src/main/java/app/ziziplayer/ui/screens/LibraryScreen.kt   focusOn, MiniProgressLine
app/src/main/java/app/ziziplayer/ui/screens/Sheets.kt          очередь: свой скролл, фикс. высота
```
