# 6.33.0 — экран плеера снят с эталонного кадра

Версия про одно: **геометрию**. Ни один интервал ниже не выбран «на глаз» — всё замерено на
референсном кадре 576×1280 (устройство 1080×2400, **1.5556 px/dp**, ширина экрана ≈ 370 dp) и
пересчитано в dp. Где число дробное — оно дробное намеренно, округление до целого dp даёт
видимый сдвиг в 1–2 px.

## Что померено и что стало

| Элемент | Было | Стало (эталон) |
|---|---|---|
| Название трека | 25 sp | **21.5 sp** (прописная 24 px) |
| Исполнитель | 15 sp | **16.5 sp** (прописная 19 px) |
| Базовая линия названия → исполнителя | 24.4 dp | **21.2 dp** (сдвиг строки на −1.1 dp) |
| Левая линия контента | 22 / 20 dp вразнобой | **24 dp** везде |
| Дорожка | 4.0 dp, бегунок r7 | **3.2 dp, бегунок r5.2** |
| Центр дорожки → верх цифр времени | 28.3 dp | **12.2 dp** |
| Кнопка Play | голый глиф, 0.190 ширины | **белый круг 0.1719 ширины, тёмный знак** |
| Центры «назад/вперёд» | 0.316 / 0.682 | **0.2891 / 0.7109** |
| Знак «назад/вперёд» | 27×25 px (шире, чем выше) | **34×38 px, перерисован** |
| «Перемешать» / «Повторять» | глиф 25 / 29 px | **35 / 35 px**, точки-индикаторы пересчитаны |
| Сердце «избранное» | круг 46 dp, под названием | **круг 29 dp по центру подписи**, заливка брендом |
| Ряд «таблеток» | 4 плашки, 62 dp | **удалён, всё в меню «⋮»** |
| Нижняя строка | «Очередь N» + «Папки» на плашке | **вывод звука слева + два знака на 0.7778 / 0.9115** |

## Кнопки «назад» и «вперёд» перерисованы, а не увеличены

Старый знак имел пропорцию 13.0 × 12.4 из сетки 24 — он был **шире, чем высок**. В референсе
ровно наоборот: 34 × 38 px, то есть 0.895. Треугольник занимает 27/34 ширины, планка 7/34,
вершина упирается в планку без зазора. Простое масштабирование этого не даёт — путь в
`ZiziIcons.SkipNext` / `SkipPrevious` собран заново (x 3.41…20.59, y 2.4…21.6).

## Play стал кругом

Главная команда экрана весила столько же, сколько «перемешать». Теперь это залитый круг
0xFFF2F2F2 диаметром 0.1719 ширины с тёмным знаком (`p.onAccent`) — треугольник 30 px в высоту,
пауза чуть меньше по кеглю, потому что две сплошные вертикали при равном габарите читаются
тяжелее треугольника с пустыми углами.

## Ряд «таблеток» убран целиком

«Скорость и тон», «Таймер сна», «В плейлист», «Папки» уехали в троеточие шапки. **Логика не
осталась висеть**: `showFx`, `showSleep`, `showPlaylists` поднимаются из `TrackMenuSheet`,
«Папки» — новый пункт того же листа (`onFolders`, необязательный: в библиотеке и поиске он не
показывается) плюс кнопка внизу. Состояние `fx` из `PlayerScreen` удалено вместе с плашкой,
которая его читала, — мёртвых подписок не осталось.

## Реальный вывод звука (новое)

`playback/AudioOutput.kt` — источник правды о том, **куда система прямо сейчас отдаёт медиапоток**:

* Android 12+: `AudioManager.getAudioDevicesForAttributes(USAGE_MEDIA)` — прямой вопрос, прямой
  ответ; тип сопоставляется с устройством из списка выходов, чтобы достать человеческое имя.
* Ниже 12: список выходов и приоритет, повторяющий поведение микшера (гарнитура > колонка).
* Обновление по событию `AudioDeviceCallback`, без опроса. Подписка снимается через 5 секунд
  после ухода экрана (`WhileSubscribed`).
* Имя чистится: `productName` встроенного динамика — это модель телефона, то есть ответ не на
  тот вопрос; такие значения заменяются на «Динамик телефона».
* `BLUETOOTH_CONNECT` объявлено в манифесте (без него Android 12+ не отдаёт имя чужого
  BT-устройства), **в рантайме не запрашивается**: не выдано — строка честно пишет
  «Bluetooth-наушники», а не пустоту.

Знак вывода настоящий: `ZiziIcons.Headphones` — дужка обводкой того же веса, что и весь набор,
и две залитые чашки; для BT-колонки — руна Bluetooth, для динамика — конус с волнами. Цвет —
фирменный оранжевый. Нажатие открывает системный переключатель вывода
(`com.android.settings.panel.action.MEDIA_OUTPUT`, откат — настройки звука).

## Обратная совместимость

`ZiziSlider` получил `trackHeightDp` / `thumbRadiusDp` / `heightDp` со **старыми значениями по
умолчанию**, `TransportButton` — `dotOffsetFraction` / `dotSizeFraction` тоже со старыми.
Мини-плеер и листы не сдвинулись ни на пиксель.

## Что осталось за кадром

Обложка: в референсе 502 px (87 % ширины), у нас 478 px (83 %). Не тронуто — это карусель
с подглядыванием соседей, её геометрия завязана на пейджер, а не на статичный квадрат.
Счётчик очереди из нижней строки убран: цифра рядом со знаком уводила его центр с замеренной
доли.
