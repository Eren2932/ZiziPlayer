# ZiziPlayer 6.10.2 + ZiziResolve 1.3 — «покрытие 100% живых треков»

**Дата:** 29 августа 2026
**versionCode:** 25 · **versionName:** 6.10.2 · **сервис:** ZiziResolve 1.3

Это релиз, в котором тракт воспроизведения впервые **измерен**, а не «ощущается работающим».

---

## Коротко

| Что | До | После |
|---|---|---|
| Покрытие выборки (24 трека) | 20/24 = 83% | **23/24 = 96%**, и 24-й физически удалён → **100% живых** |
| `resolved_proxy` | 0 (плечо никогда не доезжало до байтов) | работает, `HTTP 206`, 130–170 КБ/с |
| Приговор «трек удалён» | ставился по тексту ошибки yt-dlp | ставится только после арбитража `oembed` |
| Гео-отказ в памяти приложения | 10 минут | 60 секунд |
| Стабильность | Deno падал по OOM, «то играет, то не играет» | своп 2 ГБ + лимит V8, OOM в `journalctl -k` нет |

---

## Сервис: ZiziResolve 1.2 → 1.3

### 1. Арбитраж через `oembed` (главное)

`yt-dlp` под российским IP отвечает на гео-блок текстом `Video unavailable` —
неотличимо от удаления. Прошлый классификатор верил тексту и ставил `404` «удалён»,
что означало смерть живого трека на 10 минут.

Теперь сервер обязан спросить у YouTube, существует ли видео:

- `oembed 200` → приговор `451` «недоступен из региона сервера», **мягкий**;
- `oembed 404` → приговор `404` «удалён или закрыт», **жёсткий**;
- `oembed` молчит → мягкий, потому что арбитр не подтвердил.

Проверено: `3ohcH7nUsoc` и `jwgZOOFljZc` (YALA FUNK Slowed/Sped Up) перестали числиться
мёртвыми и заиграли через прокси. `IM4v4sq8ypo` остался мёртвым — ложноположительных нет.

### 2. `RETRY_VIA_PROXY = (451, 404, 402, 502)`

Добавлен `502`: неопознанные отказы больше не умирают на прямом пути.

### 3. Гео-маркеры расширены

Ловятся `uploader has not made this video available`, `this video is available in `
(простыня со списком стран), `you might want to use a VPN`, `error code: 152`.
Бот-чек (`403`) проверяется **до** гео, чтобы не маскировался под него.

### 4. `NEG_TTL_SOFT = 60`

Мягкие приговоры забываются за минуту. Переключил узел xray — тракт узнаёт сразу,
без ручного `/flush`. Раньше на этом наступали регулярно.

### 5. Новый эндпоинт `/geo`

```json
{"direct":{"gl":"RU","http":200},"proxy":{"gl":"DE","http":200},
 "geo_ok":true,"hint":"гео чистое, 451 должны лечиться"}
```

### 6. `/stats`: `soft_neg` / `hard_neg`

Видно, сколько приговоров были уверенными, а сколько «на всякий случай».

---

## Приложение: 6.10.1 → 6.10.2

**Локальный приговор для `451` живёт 60 секунд, а не 10 минут.**

Отказ `451` — свойство текущего выхода в интернет на сервере, а не свойство трека.
Пока приложение держало его 10 минут, оно переживало починку на сервере и врало
пользователю из своей памяти. `404/402` (удалён / только для подписчиков) по-прежнему
живут 10 минут: там арбитр подтвердил, что это правда навсегда.

---

## Инфраструктура (перенесено из «правок руками» в репозиторий)

- `server/install-zizi-resolver.sh` — своп 2 ГБ, `DENO_V8_FLAGS=--max-old-space-size=256`,
  `--workers 1`, `yt-dlp[default]` + **`yt-dlp-ejs`**, ночной крон обновления, юнит systemd.
- `server/tools/zizi-geoscan.py` — гео-скан узлов подписки: поднимает отдельный xray
  на 10801 (рабочий 1080 не трогает) и спрашивает `GL` **у самого youtube.com**.

### Находка, которая всё решила

Из 15 VLESS-узлов подписки `ipinfo` показывает Европу/США почти у всех — а YouTube
отвечает `GL=RU` у **14 из 15**. Годным оказался один: «Germany | Gemini», `GL=DE`, 374 мс.
Именно поэтому прошлый отбор узлов «по ipinfo» не давал ничего.
**Критерий выбора узла — только `GL` от youtube.com.**

---

## Что осталось неизмеренным (честно)

- Поведение очереди в приложении при `--workers 1`: резолвы последовательные, 2–6 с каждый.
  `/prefetch` должен это скрывать, но переход между треками вживую не хронометрировался.
- Скрипт установки целиком на чистой машине не прогонялся (по частям — да).
- Гео-плечо держится на одном узле. Забанят — нужен новый скан.
